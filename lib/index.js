// TODO run all test, enter point
const { describe, it, expect } = require('./src/core/function')
const Storage = require('./src/core/classes/Storage')

describe('comment', () => {
  it('add Hello + World', () => {
    expect(() => 5 + 5).toBe(10)
  })
})
